# Tests

This directory contains automated tests for app logic that can run without
board hardware.

Current automated coverage:
- `WarningThresholdPolicyTest.cpp`: focused threshold policy unit test

See `docs/04-build-run-test.md` for local build/test commands, board-only
checks, and known test gaps.
