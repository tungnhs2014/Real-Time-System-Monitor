# Tests

This directory is reserved for automated tests for the System Monitor application.

Current repository state:
- no automated test suite is committed yet
- validation is primarily manual and hardware-oriented

Recommended future coverage:
- parser/unit tests for `/proc` and `/sys` readers
- settings persistence tests
- desktop startup smoke tests
